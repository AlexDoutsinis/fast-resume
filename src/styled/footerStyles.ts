import styled from "styled-components"

export const FooterStyled = styled.footer`
  margin-top: 2.5rem;
  padding: 1rem 1.5rem;
  text-align: center;

  a {
    color: inherit;
    font-style: italic;
  }
`
