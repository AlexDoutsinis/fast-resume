import styled from "styled-components"

export const HeaderStyled = styled.header`
  padding: 1rem 0 0 1.5rem;

  h1 {
    a {
      text-align: left;
      font-size: 1.3rem;
      font-weight: 700;
      color: inherit;
    }
  }
`
